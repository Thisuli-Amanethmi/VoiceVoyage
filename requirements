Requirements

TensorFlow
pip install tensorflow

NumPy
pip install numpy

Pillow (PIL)
pip install Pillow

Seaborn
pip install seaborn

OpenCV
pip install opencv-python

import tensorflow as tf  (tensorflow - deep learning)
import numpy as np  (numpy - numerical computation)
from PIL import Image, ImageDraw, ImageFont  (pillow library - image processing)
from IPython.display import display  (to display images in the notebook)
from seaborn import color_palette  (seaborn - to bounding boxes colours - visualization)
import cv2  (opencv - computer vision)

